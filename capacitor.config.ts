import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-scroll-snap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
